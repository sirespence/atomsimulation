{
  "name": "atomsimulation",
  "version": "2.0.0",
  "scripts": {
    "build": "mkdir -p public && cp -r active prxy scripts style uv middleware.js vercel.json public/",
    "install": "echo 'ready to launch'"
  }
}
